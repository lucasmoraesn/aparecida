services:
  - type: web
    name: aparecida
    env: node
    plan: free
    buildCommand: npm run build && cd server && npm install
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
